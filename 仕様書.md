# KODATTORIA Webサイト 仕様書（現行実装ベース）

## 1. 目的
- KODATTORIAのブランド情報・イベント情報・雑誌アーカイブ・グッズ案内を、シンプルな1ページ型Webサイトとして公開する。
- 日本語・英語・イタリア語の3言語切り替えに対応し、静的HTML/CSS/JS構成で運用しやすいことを重視する。

## 2. サイト構成
- トップページ: [index.html](index.html)
- 雑誌アーカイブページ: [magazine.html](magazine.html)
- グッズページ: [goods.html](goods.html)
- 共通スタイル: [style.css](style.css)
- 言語切り替え処理: [lang.js](lang.js)

## 3. 画面構成
### 3.1 共通レイアウト
- 画面上部に言語切り替えバーを配置する。
- 左カラムに写真ボックスとサイトナビゲーションを配置する。
- 右カラムにメインフォメーションを配置する。
- 画面下部にフッターを配置する。

### 3.2 左カラム
- 写真枠を表示するプレースホルダー領域を設ける。
- 「トップ」「雑誌アーカイブ」「グッズ」へのリンクを表示する。
- 現在のページに応じて、ナビゲーションの該当項目をアクティブ表示する。

### 3.3 右カラム
- サイト名とキャッチコピーを表示する。
- 各ページごとにセクション見出しと本文を表示する。
- Instagramリンク・QR画像・問い合わせ案内を表示する。

## 4. 機能要件
### 4.1 言語切り替え
- 日本語、英語、イタリア語の3言語を切り替え可能とする。
- 言語切り替えボタンを押すと、body要素に class名 `lang-ja` / `lang-en` / `lang-it` を付与する。
- 各表示テキストは `data-lang` 属性で言語別に管理し、CSSで表示/非表示を切り替える。

### 4.2 ナビゲーション
- 3ページ間で相互リンクを提供する。
- 現在ページに対応するナビ項目を強調表示する。

### 4.3 レスポンシブ対応
- 画面幅が狭い場合は、左右カラムを縦並びに切り替える。
- モバイル表示では、写真枠やナビゲーションが自然に折りたたまれるようにする。

## 5. 各ページの内容
### 5.1 トップページ
- 次回営業日を表示する。
- 「KODATTORIAとは」の説明文を表示する。
- メンバー一覧を表示する。
- Instagram情報を表示する。

### 5.2 雑誌アーカイブページ
- 雑誌の一覧をカード風レイアウトで表示する。
- 各巻のタイトル・テーマ・発行日を表示する。
- 例として `vol.1` 〜 `vol.8` の情報を掲載する。
- 購入方法について、現地のみでオンライン販売なしという注記を掲載する。

### 5.3 グッズページ
- グッズ商品「オレキエッT」を1件表示する。
- 写真準備中・価格未定のプレースホルダー表示を行う。
- 購入方法は現地のみとする。

## 6. デザイン仕様
- 全体のフォントは明朝体を採用し、シンプルで紙媒体に近い印象を持たせる。
- 背景にはロゴ画像を透かしで繰り返し表示する。
- リンクは青系・紫系・赤系で状態差を表現する。
- セクション見出しは灰色背景＋左線で強調する。

## 7. データ構造・実装方針
- HTMLは静的コンテンツとして作成する。
- 言語切り替えはJavaScriptで制御する。
- 共通デザインはCSSで管理する。
- 画像はプレースホルダーとして枠・テキスト表示を行う構造とする。

## 8. 制約事項
- サーバー依存のない静的サイトとして構成する。
- 現在の実装は外部データベースやCMS連携を前提としていない。
- 更新時はHTMLファイルとCSS/JSを直接編集する運用を想定する。
